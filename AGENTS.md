# 🏥 건강 체크 어플 프로젝트 전역 개발 지침서 (AGENTS.md)

본 지침서는 프로젝트의 개발 제약 조건과 구현 방향을 관리하는 명세서입니다. 내일 출근 후 즉시 개발을 이어갈 수 있도록 상세 사양을 여기에 정의합니다.

---

## 🚫 1. 영구 규칙 및 범위 제외 사항 (Scope Exclusions)
1. **소셜 로그인 제거**: 카카오/네이버 간편 로그인 기능은 프로젝트 범위에서 완전히 제외되었습니다. 소셜 로그인 관련 신규 버튼 및 API는 재개발하지 않습니다.
2. **스마트 워치 연동 제외**: 기획 및 개발 범위에서 스마트 워치 연동 사양은 영구적으로 누락 및 스킵 처리합니다.
3. **실물 팩스/유료 SMS 연동 제외**: 타사 유료 API 게이트웨이 연동은 제외하며, 인앱 모션 시뮬레이션 및 이메일 모의 발송 등으로 대체합니다.

---

## 📅 2. 내일 즉시 시작할 주요 개발 요건 (Roadmap & Action Plan)

### 📌 태스크 A: 비로그인 상태 시 사용자 잔존 데이터 원천 격리 (Zero Cache)
* **목표**: 로그인 상태가 아니거나 로그아웃을 실행했을 때, 이전 사용자의 개인 정보 및 기록들이 화면에 노출되지 않도록 전역 Zustand 스토어를 완전히 초기화합니다.
* **초기화 대상**:
  1. **알림 및 로컬 푸시**: `useNotificationStore` 초기화 및 예약 취소.
  2. **장바구니**: `useCartStore` 내의 모든 아이템 비우기.
  3. **진단 기록**: `useSymptomStore` 내의 자가진단 히스토리(`history`) 전체 클리어.
  4. **GPS 및 보호자 정보**: `useGpsStore` 의 보호 대상자 전화번호 및 반경 설정 리셋 (`resetGpsStore` 연동).

### 📌 태스크 B: "기존 진료 이어받기" E2E 선택 및 연동 흐름 구현
* **진입 포인트**: `app/(tabs)/clinic.tsx` -> **기존 진료 이어받기** 버튼
* **구현 사양**:
  1. **진단 이력 조회**: 사용자의 가장 최근 자가진단 기록(`useSymptomStore.getState().history[0]`)을 조회합니다.
  2. **분기 모달/페이지 노출**: 최근 진단 정보가 존재하는 경우, 유저에게 **대면**과 **비대면** 중 진료 방식을 묻는 팝업창을 구현합니다.
  3. **대면 진료 선택 시**:
     * 자가진단 결과가 연동된 채로 **대면 병원 예약 화면**(`app/clinic-hospitals.tsx`로 `fromDiagnosis: 'true'`, `reportId: record.id` 파라미터 연동)으로 이동하여 맞춤 진료과목 필터링이 적용된 병원 예약을 태웁니다.
  4. **비대면 진료 선택 시**:
     * 해당 진단 결과지가 자동으로 탑재된 상태로 **비대면 화상 진료 신청 Flow**(`telemedicine` 세션 생성 및 대기열)로 즉시 묶어서 바로 예약을 접수하고 대기 화면으로 진입시킵니다.

### 📌 태스크 C: `expo-av` 라이브러리 지원 중단 대비 마이그레이션
* **목표**: Expo SDK 54 지원 중단(Deprecated)에 대비하여, 기존 `expo-av` 패키지를 들어내고 `expo-audio` 및 `expo-video` 신규 표준 라이브러리로의 대체를 준비합니다.
* **이슈 파트**: `app/(tabs)/gps.tsx` 내의 비상 사이렌 소리(playsInSilentModeIOS 등) 연동부
* **액션**: `expo-audio` 의 `AudioPlayer` 또는 최신 표준 인터페이스로 마이그레이션하여, 향후 네이티브 빌드 시 경고 소멸 및 사이렌 소리가 에러 없이 원활하게 구동되도록 리팩토링 진행.

---

## 🛠️ 3. 현재 E2E 복구 및 정상 작동 상태 검증
* **컴파일 검사**: `npx tsc --noEmit` 실행 시 **에러 0건 (Build Success)** 통과 확인.
* **비대면 진료 버튼 상태**: 롤백을 통해 예전 E2E 100% 버전의 비대면 진료(`telemedicine`) 및 협력 병원 예약 기능이 온전하게 복원되어 잘 살아 있습니다.
